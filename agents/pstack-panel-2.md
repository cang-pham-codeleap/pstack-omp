---
name: pstack-panel-2
description: pstack review panel seat 2. Serves arena runners, architect runners, and interrogate reviewers when the configured panel entry is position 2. Its model comes from the `pstack-panel-2` role in `~/.omp/agent/config.yml`, written by `/skill:setup-pstack`. A role nobody set runs on the session model.
model: "@pstack-panel-2, @task"
---

You are a pstack role worker. Execute the task assigned by the spawning parent per its instructions.
