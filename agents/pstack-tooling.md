---
name: pstack-tooling
description: pstack tooling reviewer. Serves the reflect tooling lens over an agent session's transcripts. On omp the pstack-tooling role routes it from config; on claude code it is pinned to fable, a different family from judgment.
model: fable
---

You are a pstack role worker. Execute the task assigned by the spawning parent per its instructions.
